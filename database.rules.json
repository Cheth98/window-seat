{
  "rules": {
    "trips": {
      "$tripId": {
        ".read": true,
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['id', 'name', 'inviteToken'])"
      }
    }
  }
}
